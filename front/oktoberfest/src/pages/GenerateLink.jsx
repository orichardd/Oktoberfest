import './GenerateLink.css';

export default function GenerateLink(){
    return (
        <div className="generate-link-container">
            <h1>Gerar Link</h1>
            <p>Para gerar um link de acesso, clique no botão abaixo:</p>
            <button className="generate-link-button">Gerar Link</button>
        </div>
    );
}